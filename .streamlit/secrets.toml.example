# Streamlit Community Cloud Secrets Configuration
# Copy this to the Streamlit Cloud dashboard under "Secrets" tab
# DO NOT commit the actual secrets.toml file with real keys

# Optional: Fallback OpenAI API Key (users can also enter their own in the app)
OPENAI_API_KEY = ""

# Optional: Other API keys if needed
# GOOGLE_MAPS_API_KEY = ""
# POSTCODE_API_KEY = ""