# Streamlit Secrets Configuration
# ==================================
# This file shows the structure for configuring API keys
# DO NOT commit the actual secrets.toml file with real keys to git

# OPTION 1: Configure in Streamlit Cloud (Recommended)
# -----------------------------------------------------
# 1. Go to your Streamlit Cloud app dashboard
# 2. Click on "Settings" > "Secrets"
# 3. Paste the content below with your actual API key
# 4. Save - the app will restart automatically

# OPTION 2: Local Development
# ----------------------------
# 1. Create a file named "secrets.toml" in this .streamlit directory
# 2. Copy the content below and add your actual API key
# 3. The file is already in .gitignore so it won't be committed

# Configuration:
# --------------
# If you set OPENAI_API_KEY here, users won't need to enter it each session
# If not set, users will be prompted to enter their own API key

OPENAI_API_KEY = "sk-your-api-key-here"

# Optional: Other API keys if needed in future
# GOOGLE_MAPS_API_KEY = ""
# POSTCODE_API_KEY = ""